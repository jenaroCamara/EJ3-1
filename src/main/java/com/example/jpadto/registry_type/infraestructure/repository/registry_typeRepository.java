package com.example.jpadto.registry_type.infraestructure.repository;

import com.example.jpadto.registry_type.domain.Registry_type;
import org.springframework.data.jpa.repository.JpaRepository;

public interface registry_typeRepository extends JpaRepository<Registry_type, String> {
}
